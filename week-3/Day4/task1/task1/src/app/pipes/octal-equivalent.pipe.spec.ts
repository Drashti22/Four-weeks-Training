import { OctalEquivalentPipe } from './octal-equivalent.pipe';

describe('OctalEquivalentPipe', () => {
  it('create an instance', () => {
    const pipe = new OctalEquivalentPipe();
    expect(pipe).toBeTruthy();
  });
});
